- name: git checkout from github
  ansible.builtin.git:
    repo: git@github.com:organizaion/ansible.git
    dest: /root/ansible
    update: no
    accept_newhostkey: true
  environment:
    GIT_TERMINAL_PROMPT: "0"